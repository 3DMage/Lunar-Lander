namespace LunarLander.Input
{
    // Label for states for input configuration.
    public enum ConfigState
    {
        VALID,
        ALREADY_MAPPED,
        ITSELF,
        CANCELED,
        NOTHING
    }
}
